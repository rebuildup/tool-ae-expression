# Auxiliary project-local tooling for imported Agent Skills.
# Host repository remains the source of full application validation tooling.

[tools]
bun = "1.4.2"

[tasks.skills-list]
run = "bunx skills add rebuildup/project-init --list"

[tasks.skills-bootstrap]
run = "bunx skills add rebuildup/project-init --skill '*' --agent claude-code --agent codex -y"

[tasks.skills-update]
run = "bunx skills update -p -y"
